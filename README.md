# bundle — generated output

> **Do not hand-edit this branch.**

This branch is populated automatically by `scripts/build-bundle.mjs`.
Run that script from the superproject (`main` branch):

```sh
node scripts/build-bundle.mjs          # assemble locally
node scripts/build-bundle.mjs --push   # assemble + push
```
